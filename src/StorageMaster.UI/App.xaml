<Application
    x:Class="StorageMaster.UI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Provides Fluent Design control styles for WinUI 3 -->
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <ResourceDictionary Source="Styles/Tokens.xaml" />
                <ResourceDictionary Source="Styles/Typography.xaml" />
                <ResourceDictionary Source="Styles/Cards.xaml" />
                <ResourceDictionary Source="Styles/Buttons.xaml" />
                <ResourceDictionary Source="Styles/Inputs.xaml" />
                <ResourceDictionary Source="Styles/Navigation.xaml" />
                <ResourceDictionary Source="Styles/Motion.xaml" />
                <ResourceDictionary Source="Styles/Severity.xaml" />
                <ResourceDictionary Source="Styles/FileTypes.xaml" />
                <ResourceDictionary Source="Styles/Icons.xaml" />
                <!--
                    Must stay last: merged dictionaries are searched last-to-first,
                    and Palette.xaml deliberately overrides keys that
                    XamlControlsResources also defines.
                -->
                <ResourceDictionary Source="Styles/Palette.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!--
                A neutral metadata chip. This used to be filled with the accent,
                which spent the app's one attention colour on facts like a volume
                label; accent now means state, selection or primary action only.
            -->
            <Style x:Key="StatusChipStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource SmSurfaceSunkenBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource SmStrokeSubtleBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="{StaticResource ChipCornerRadius}" />
                <Setter Property="Padding" Value="{StaticResource ChipPadding}" />
            </Style>

            <Style x:Key="StatusChipTextStyle" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
                <Setter Property="Foreground" Value="{ThemeResource SmTextSecondaryBrush}" />
                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                <Setter Property="TextWrapping" Value="NoWrap" />
            </Style>

            <Style x:Key="EmptyStateTextStyle" TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
                <Setter Property="TextWrapping" Value="WrapWholeWords" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
